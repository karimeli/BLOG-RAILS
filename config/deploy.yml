service: blog
image: karim848/blog

servers:
  web:
    - 148.229.5.96

registry:
  username: karim848
  password:
    - KAMAL_REGISTRY_PASSWORD

volumes:
  - "blog_storage:/rails/storage"
  - "blog_db:/rails/db"

builder:
  arch: amd64

# ¡LA SECCIÓN QUE ACABAS DE AGREGAR!
ssh:
  user: karim